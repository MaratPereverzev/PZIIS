# Версии зафиксированы точно: воспроизводимость сборки и контроль над тем,
# какой именно код зависимостей попадает в образ.
fastapi==0.115.6
uvicorn[standard]==0.34.0
psycopg[binary,pool]==3.2.3
argon2-cffi==23.1.0
cryptography==44.0.0
jinja2==3.1.5
python-multipart==0.0.20

# Средства проверки. В образ входят намеренно: проверки защищённости
# запускаются в том же окружении, в котором работает приложение.
pytest==8.3.4
httpx==0.28.1
